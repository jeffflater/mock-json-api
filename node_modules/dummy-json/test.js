var dummyjson = require('./dummy-json');
var fs = require('fs');
var Handlebars = require('handlebars');

var test = fs.readFileSync('test.json', {encoding: 'utf8'});

var data = {
  cow: 'moo',
  animals: [
    'goat',
    'pig',
    'sheep',
    'chicken'
  ],
  companies: ['c-one', 'c-two', 'c-three']
};

var firstNames = ['Mr', 'Mrs'];
var lastNames = ['Badger', 'Fox'];
var companies = ['Skynet', 'Earthnet'];

var helpers = {
  donkey: function() {
    return 'donkey';
  }
  // boolean: function() {
  //   console.log('override boolean!!');
  //   return 'YES';
  // }
};


var result = dummyjson.parse(test, {
  data: data,
  helpers: helpers
  // firstNames: firstNames,
  // lastNames: lastNames,
  // companies: companies
});

console.log('----------');
console.log(result);
